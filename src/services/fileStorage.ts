// Full implementation in Phase 7.
// Stubs are here so Phase 6 TypeScript compilation passes.

/**
 * Appends an org-mode entry string to the configured .org file.
 * Platform-branched: SAF-X on Android, RNFS on iOS.
 */
export async function appendToOrgFile(_orgEntry: string): Promise<void> {
  throw new Error('fileStorage not yet implemented — coming in Phase 7');
}
